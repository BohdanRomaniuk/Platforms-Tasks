<Window x:Class="_5__Hexagones.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_5__Hexagones"
        mc:Ignorable="d"
        Title="Hexagones" Height="550" Width="750" Icon="hexagon.ico">
    <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Header="New" Command="{Binding ClearWindow_Command}"/>
                    <MenuItem Header="Open" Command="{Binding OpenFile_Command}"/>
                    <MenuItem Header="Save" Command="{Binding SaveFile_Command}"/>
                    <Separator />
                    <MenuItem Header="Exit" Command="{Binding CloseWindow_Command}" />
                </MenuItem>
                <MenuItem Header="Shapes">
                    <MenuItem Header="1" />
                </MenuItem>
            </Menu>
        <Canvas Name="surface" Background="Transparent" DockPanel.Dock="Bottom" 
             MouseMove="Canvas_MouseMove" 
             MouseLeftButtonDown="Canvas_MouseLeftButtonDown"  
             MouseLeftButtonUp="Canvas_MouseLeftButtonUp"/>
        </DockPanel>
</Window>
